# Quantis

### 啟動容器

```bash
docker compose -f docker-compose.dev.yml up --build -d
```

### 前端入口

[http://localhost:5173](http://localhost:5173)

### 後端路由文件

[http://localhost:8080/swagger]([http://localhost:8080/swagger)

如果前端需取用後端入口可以直接使用`VITE_API_URL`常數
